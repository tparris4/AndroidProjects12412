<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="start"
    android:orientation="vertical"
    android:stretchColumns="1">

    <TableRow>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_span="2"
            android:text="CurrencyConverter"
            android:visibility="visible" />
    </TableRow>


    <TableRow>

        <TextView android:text="Currency1"
            style="@style/NiceText"/>
        <EditText
            android:id="@+id/numberEditText"
            android:inputType="number" />

    </TableRow>

    <Spinner
        android:id="@+id/spinner_currency"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:dropDownWidth="match_parent"
        android:entries="@id/spinner_currency"
        android:minWidth="@android:dimen/app_icon_size"
        android:spinnerMode="dropdown"
        android:visibility="visible" />


    <TableRow>

        <TextView android:text="Currency2"
            style="@style/NiceText"/>

        <EditText
            android:id="@+id/numberEditText2"
            android:inputType="number" />

    </TableRow>

    <TableRow>
        <TextView android:text="New Currency"
            android:id="@+id/resultC"/>



    </TableRow>

</TableLayout>